# Database
# PostgreSQL connection string
DATABASE_URL=postgresql://user:password@localhost:5432/pdv_roma

# Security
# Master key for encryption (32+ characters recommended)
# Generate with: openssl rand -hex 32
MASTER_KEY=your-32-char-master-key-here-change-in-production

# Session secret for JWT tokens
# Generate with: openssl rand -hex 32
SESSION_SECRET=your-session-secret-here-change-in-production

# App Configuration
# Environment: development, production, or test
NODE_ENV=development

# Port (optional, Next.js defaults to 3000)
# PORT=3000
